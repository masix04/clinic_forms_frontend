import { Component, Injectable } from "@angular/core";
// import { HttpClient } from "@angular/common/http";
// import { inputComponent } from "../inputs/inputs.component";
@Component({
    
})
@Injectable()
export class inputHelperService{
    // Patient = inputComponent;
    // constructor(public Patient: inputComponent)
    // {

    // }
    /** POST: add a new hero to the database */
    addPatientRecord(patient: any) {
        // return this.http.post<this.Patient>()
        // .pipe(
        //     catchError(this.handleError('addHero', hero))
        // );
        return null;
    }
}